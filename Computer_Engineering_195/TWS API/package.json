{
  "name": "tws-api",
  "version": "1.0.0",
  "description": "Endpoint from which we can receive/serve data sent from the UiPath RPA.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Brian Tamsing",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "multer": "^1.4.2"
  }
}
